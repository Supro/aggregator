<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Показать меню</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/redaction"></a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        {{#if session.currentUser.isChiefEditor}}
          <li>{{link-to "Страницы" 'pages.show' 1}}</li>
          <li>{{link-to "Рекомендации" 'recommendations.index'}}</li>
          <li>{{link-to "Источники" 'sources'}}</li>
          <li>{{link-to "Редакция" 'publications'}}</li>
          <li>{{link-to "Пользователи" 'users'}}</li>
        {{/if}}
        {{#if session.currentUser.isEditor}}
          <li>{{link-to "Редакция" 'publications'}}</li>
        {{/if}}
        {{#if session.currentUser.isJournalist}}
          <li>{{link-to "Публикации" 'publications'}}</li>
        {{/if}}
        {{#if session.currentUser.isJuniorJournalist}}
          <li>{{link-to "Публикации" 'publications'}}</li>
        {{/if}}
        {{#if session.isAuthenticated}}
          <li>{{link-to "Ссылки" 'urls'}}</li>
        {{/if}}
        {{#unless session.isAuthenticated}}
          <li>{{link-to "Войти" 'login'}}</li>
        {{/unless}}
        {{#if session.isAuthenticated}}
          <li><a href='/logout' {{action 'logout'}}>Выйти {{session.currentUser.name}}</a></li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>

{{menu-hack}}
